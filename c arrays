include <stdio.h>

int main()
{
	// array = data structure that can store many values of the same data type.

	double prices[5] = {5.0 10.0, 15.0, 20.0};
// "%1f" format specifier for double
	printf("%1f", prices [0]);
	
	return 0;
}